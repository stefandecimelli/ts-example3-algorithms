A small app that uses vanilla JS/TS without React. 
Unfortunately there's no escaping webpack, unless you want your interdependant ts files to turn into spaghetti.

To run, just execute:
```
npm run start
```
